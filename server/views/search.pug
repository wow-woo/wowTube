extends layouts/main
include mixins/videoBlock

block content 
    .search_header
        h3 Searching for 
            span #{searchingBy}
    .search_videos
        each video in videos
            +videoBlock({
                title:video.title,
                views:video.views,
                videoURL:video.videoURL
            })
