mixin videoBlock(video={})
    a(href=routes.video_detail(video.id))
        .videoBlock
        video.videoBlock__thumbnail(src=video.videoURL, controls=true,muted=true, autoplay=true )
        h4.videoBlock__title=video.title
        p.videoBlock__views=video.views
        p.videoBlock__views=video.description